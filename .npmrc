# Security settings
trust-policy=no-downgrade
minimum-release-age=1440

# Prefer workspace packages over registry versions
prefer-workspace-packages=true

# Save exact versions to avoid surprises
save-exact=true
